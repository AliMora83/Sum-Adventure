# Copy to .env.local and fill in. .env.local is gitignored; never commit
# real keys.

# Sprint 4 — enquiry form
RESEND_API_KEY=
ENQUIRY_TO_EMAIL=
ENQUIRY_FROM_EMAIL=

# Sprint 4.5 — canonical site origin for metadataBase and OG image URLs.
# Set once the client's domain is live. Falls back to Vercel's own
# preview/production URL, then localhost — safe to leave empty until then.
NEXT_PUBLIC_SITE_URL=
